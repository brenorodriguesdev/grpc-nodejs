syntax = "proto3";

service LoginService {
    rpc auth (AuthRequest) returns (AuthResponse) {}
}

message AuthRequest {
    string username = 1;
    string password = 2;
}

message AuthResponse {
    string token = 1;
}